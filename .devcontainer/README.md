### Docker周り
```
"docker/mysql/data" データ永続化フォルダ
"docker/mysql/my.cnf" mysqlの設定ファイル
"docker/workspace/Dockerfile" docker imageの指定、Java・Gradleのバージョン管理

"docker-compose.yml" phpmyadmin・mysqlのimage等指定
```
